export {
  colors,
  highContrastColors,
  typography,
  radii,
  spacing,
  breakpoints,
  layout,
  watermark,
  contrast,
  defaultTheme,
  highContrastTheme,
  type Theme,
} from './tokens';

export { ThemeProvider, useTheme } from './ThemeProvider';

export {
  getFontSizeForGrade,
  getGradeCategory,
  useGradeFontSize,
  type Platform,
  type GradeCategory,
} from './font-size';

export {
  theme,
  SUBJECTS,
  GRADES,
  GENDERS,
  RELATIONSHIPS,
  PARENT_RELATIONSHIPS,
  type UITheme,
} from './uiTheme';
