/**
 * Border radius values for ChikuMiku LearnVerse Mobile
 *
 * Matches the web client design system exactly.
 *
 * Validates: Requirements 23.3
 */

export const borderRadii = {
  /** Pill-shaped buttons (22dp) */
  button: 22,
  /** Primary content cards (16dp) */
  card: 16,
  /** Tags, badges, inline info boxes (10dp) */
  badge: 10,
  /** Form inputs (8dp) */
  input: 8,
  /** Small elements (4dp) */
  small: 4,
} as const;
